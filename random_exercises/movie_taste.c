#include <stdio.h>
#define SIZE 10
int mostSimilar(int arr[SIZE][SIZE],int numUser,int numMovies);
int main(void){
	arr[SIZE][SIZE]={{4,4,3,3,2},{4,3,1,1,5},{5,5,5,2,2},{4,3,1,1,4}};
}

int mostSimilar(int arr[SIZE][SIZE],int numUser,int numMovies){
	int counter,int other_counter;
	for(counter=0;counter<numUser;counter++){
		for(other_counter=0;other_counter<numMovies;other_counter++){
			//other_counter
			//calculate distance
		}
		// if the distance is less than the max distance then replace it
	}


}